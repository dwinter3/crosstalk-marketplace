{
  "name": "crosstalk-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "crosstalk inter-peer MCP — screened SQS messaging via Cognito-vended scoped creds.",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "esbuild src/server.js --bundle --platform=node --format=esm --outfile=dist/server.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  }
}
